"""Репозитории для доступа к данным.

Пакет разделён на `interfaces` (протоколы, не зависящие от конкретной СУБД)
и `implementations` (конкретные реализации на SQLAlchemy). Сервисы
application-слоя зависят только от интерфейсов, что позволяет подменять
реализацию хранения данных без изменения бизнес-логики.
"""
